import {
	ALERT_TRIGGER
} from './../constants';

const alertoState = {
	data : null
};

function alerto(state = alertoState, action) {
	switch(action.type)  {
		default:
			return state;
		break;
	}
}

export default alerto;